%%(1) 启动并停止Erlang shell。

%%在系统终端中输入erl启动erlang shell.
%%在系统终端用ctrl+G重启erlang shell终端.
%%在系统终端用ctrl+C重启erlang shell终端.

%%(2) 在Erlang shell终端输入一些命令。不要忘了以句号和空白结束命令。
%%c(Mod).

(3) 对hello.erl做一些小小的改动。在shell里编译并运行它们。如果有错，中止Erlang shell
并重启shell。
